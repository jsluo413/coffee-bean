# Python
__pycache__/
*.pyc
*.pyo
.venv/
.env
.ipynb_checkpoints/

# Tooling
.streamlit/secrets.toml
.DS_Store
.vscode/
.idea/

# OS / IDE noise
Thumbs.db
desktop.ini
~$*           # MS Office lock files

# Tool-specific local config
.claude/

# Local build / orchestration scripts (kept out of the published repo;
# the deliverables — Streamlit app, write-up, deck — are checked in directly).
src/build_pptx.py
src/compute_findings.py
src/export_figures.py
src/embed_figures.py
docs/findings.json
docs/presentation.pptx
# docs
docs/
# Large local exports (PNGs are committed for GitHub Pages fallback;
# leave them in unless they balloon)
